import { StyledTitle } from '@/components/Title/StyledTitle';
import { Container } from '@/components/Title/Container';

export const Title = () => {
  return (
    <Container>
      <StyledTitle>Исторические даты</StyledTitle>
    </Container>
  );
};
